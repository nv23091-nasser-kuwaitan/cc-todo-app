Feature: Filters & Sorting

Status: Implemented (server-side)

Details:
- `GET /api/tasks` supports query params: `status`, `priority`, `category_id`, `q`, `sort`, `page`, `per_page`.
- `/api/tasks/search` supports pagination and sorting.

Notes:
- This branch records a minimal marker file to represent the `filters-sorting` feature branch for PR/demo purposes.
